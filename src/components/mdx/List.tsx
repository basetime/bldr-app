import styled from 'styled-components'

const LI = styled.li`
  li {
    line-height: 1.75rem;
    font-family: Roboto
  }

`

const OL = styled.ol`

    li {
      line-height: 1.75rem;
      font-family: Roboto
    }

`

const UL = styled.ul`

    li {
      line-height: 1.75rem;
      font-family: Roboto
    }

`

const List = {
  LI,
  UL,
  OL
}

export default List;